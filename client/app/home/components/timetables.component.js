var moment = require('moment');

module.exports = {
    templateUrl: '/static/app/home/components/timetables.template.html',
    bindings: {
        selectedDay: '<?'
    },
    controller($scope) {
    }
};